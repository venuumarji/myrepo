pipeline {
  agent {label 'tomcat'}
  stages {
    stage('check') {
      steps {
              sh 'pwd'
              sh 'echo "hi good morning"'
              sh 'pwd'
              sh 'free'
      }
    }
  }
}    
